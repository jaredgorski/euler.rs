  ____________
  | EULER.RS |
  ~~~~~~~~~~~~

Project Euler solutions implemented in Rust.

NOTE: use `rustc` to compile binaries rather than cargo, since this is a library of solutions rather than a cohesive program.

